{
  "name": "mini-auth-server-jwt",
  "module": "./src/app.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.3",
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "prisma": "^6.16.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jose": "^6.1.0",
    "pino": "^10.0.0",
    "zod": "^4.1.12"
  },
  "scripts": {
    "build": "bun build ./src/app.ts --outdir ./build",
    "clean": "rm -rf build && echo 'Done.'",
    "dev": "bun --watch run ./src/app.ts",
    "test": "jest",
    "prisma:setup": "bun prisma generate && bun prisma migrate deploy && bun prisma db push",
    "prisma:migrate": "bun prisma migrate dev --name init"
  }
}
